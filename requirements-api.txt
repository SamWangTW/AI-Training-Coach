# Dependencies for the containerized API service only.
# Subset of pyproject.toml — excludes streamlit (ui/) and seleniumbase
# (browser automation for Garmin sync, intentionally left out of Docker).
langgraph>=0.2
langchain-anthropic>=0.3
langchain-mcp-adapters>=0.1
langchain-core>=0.3
anthropic>=0.40
mem0ai>=0.1
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.28
python-dotenv>=1.0
pydantic>=2.0
